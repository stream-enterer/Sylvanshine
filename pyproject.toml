[project]
name = "sylvanshine-build"
version = "0.1.0"
description = "Build tools for Sylvanshine"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24.0",
    "Pillow>=10.0.0",
    "polars>=1.0.0",
    "rich>=13.0.0",
]

[dependency-groups]
dev = []

[tool.uv]
cache-dir = "./.uv-cache"
